{% extends 'base.html.twig' %}

{% block title %}ConsultationPatient{% endblock %}

{% block body %}
    <h1>ConsultationPatient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultation_patient.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ consultation_patient.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ consultation_patient.surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consultation_patients_index') }}">back to list</a>

    <a href="{{ path('app_consultation_patients_edit', {'id': consultation_patient.id}) }}">edit</a>

    {{ include('consultation_patients/_delete_form.html.twig') }}
{% endblock %}
